1. Cuando no deje actualizar kali debido a llaves deprecadas, usar este comando y volver a ejecutar la actualizacion:
    wget http://archive.kali.org/archive-key.asc -O /etc/apt/trusted.gpg.d/kali-archive-key.asc
  y despues volver a ejecutar:
    sudo apt-get update
